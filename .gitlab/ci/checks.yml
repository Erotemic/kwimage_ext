# Autogenerated by xcookie.
# Project-owned source checks are intentionally isolated from the
# anchor-heavy primary GitLab pipeline.
check/backend-parity:
  stage: test
  image: python:3.13
  tags:
  - docker
  - linux-x86_64
  - build
  - kitware-python-stack
  variables:
    PIP_CACHE_DIR: $CI_PROJECT_DIR/.cache/pip
    UV_CACHE_DIR: $CI_PROJECT_DIR/.cache/uv
  except:
    refs:
    - release
  cache:
    paths:
    - .cache/pip
    - .cache/uv
  script:
  - curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --profile minimal
  - export PATH="$HOME/.cargo/bin:$PATH"
  - rustc --version
  - cargo --version
  - python -m pip install -e '.[tests]'
  - ./dev/check_backend_parity.sh
